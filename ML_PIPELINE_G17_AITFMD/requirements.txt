numpy>=1.26,<2
pandas
PyYAML
spectral
scikit-image
scikit-learn
matplotlib
tqdm
PyWavelets

# Deep learning
# x86_64 Mac: pip naturally caps at 2.2.2 (no newer wheels on PyPI)
# arm64 Mac / Linux CUDA: gets latest available
torch>=2.0
torchvision

# ONNX export (torch.onnx.export on PyTorch 2.x)
onnx
onnxscript
